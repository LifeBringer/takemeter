# secrets
.env
*.key

# python
.venv/
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# model weights (exceed GitHub 100MB limit) + HF cache
model/
*.safetensors
*.bin
.hf_cache/
checkpoints/
results/

# os
.DS_Store

# local agent config (machine-specific permissions / paths)
.claude/settings.local.json

# raw video (keep only the down-converted demo)
demo/*.mov
demo/raw_*.mp4

# intermediate labeling batches (regenerable from candidates.csv)
data/batches/
